machine:
  xcode:
    version: "9.0"

dependencies:
  cache_directories:
    - "Carthage/"
    - "~/.cocoapods"

test:
  override:
    - script/test iphonesimulator "platform=iOS Simulator,name=iPhone 6s" DrawerKit build
